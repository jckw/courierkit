# @courierkit/core

Shared primitives used across CourierKit packages.

## Exports

- `Interval` — half-open `[start, end)` time interval
- `DateRange` — semantic alias for `Interval`
- `DurationMs` — milliseconds duration type
